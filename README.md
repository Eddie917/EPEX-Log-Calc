# EPEX Logistics Calculator (Next.js + Netlify)

## Lokálne
```bash
npm install
npm run dev
# http://localhost:3000
```

## Netlify
- Build command: `npm run build`
- Publish directory: `.next`
- Plugin: `@netlify/plugin-nextjs` (v `netlify.toml`)
- (env) `NODE_VERSION=20`
